#include<iostream>
using namespace std;
int main(){
    int a;
    if(3 + 2%5) cout<<"This Works"<<endl;
    if(-5) cout<<"Even This Works"<<endl;
    if(a=5) cout<<"Surprisingly Even This Works"<<endl;
// in if statement if the expression is true or any number expect 0 will run the condition
}